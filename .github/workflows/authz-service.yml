name: Authz CI

on:
  push:
    paths:
      - '.github/workflows/authz-service.yml'
      - '.github/workflows/template-ci.yml'
      - 'apps/authz/**'
      - 'libs/**'
      - 'package.json'
      - 'yarn.lock'
      - 'jest.config.json'
      - 'eslint.config.js'
      - 'nest-cli.json'
      - 'tsconfig.json'
      - 'tsconfig.build.json'
  pull_request:
    paths:
      - '.github/workflows/authz-service.yml'
      - '.github/workflows/template-ci.yml'
      - 'apps/authz/**'
      - 'libs/**'
      - 'package.json'
      - 'yarn.lock'
      - 'jest.config.json'
      - 'eslint.config.js'
      - 'nest-cli.json'
      - 'tsconfig.json'
      - 'tsconfig.build.json'

jobs:
  authz-pipeline:
    uses: ./.github/workflows/template-ci.yml
    with:
      service-name: 'authz'
    secrets:
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
