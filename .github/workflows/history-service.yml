name: History CI

on:
  push:
    paths:
      - '.github/workflows/history-service.yml'
      - '.github/workflows/template-ci.yml'
      - 'apps/history/**'
      - 'libs/**'
      - 'package.json'
      - 'yarn.lock'
      - 'jest.config.json'
      - 'eslint.config.js'
      - 'nest-cli.json'
      - 'tsconfig.json'
      - 'tsconfig.build.json'
  pull_request:
    paths:
      - '.github/workflows/history-service.yml'
      - '.github/workflows/template-ci.yml'
      - 'apps/history/**'
      - 'libs/**'
      - 'package.json'
      - 'yarn.lock'
      - 'jest.config.json'
      - 'eslint.config.js'
      - 'nest-cli.json'
      - 'tsconfig.json'
      - 'tsconfig.build.json'

jobs:
  history-pipeline:
    uses: ./.github/workflows/template-ci.yml
    with:
      service-name: 'history'
    secrets:
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
